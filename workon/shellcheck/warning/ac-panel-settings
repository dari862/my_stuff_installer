
In /home/dari/Desktop/my_stuff/bin/openbox/pipemenu/ac-panel-settings line 16:
		styles=(`find $polybar_dir/* -maxdepth 1 -type d -not -name "*-Bitmap" -not -name panels`)
                        ^-- SC2207 (warning): Prefer mapfile or read -a to split command output (or quote to avoid splitting).


In /home/dari/Desktop/my_stuff/bin/openbox/pipemenu/ac-panel-settings line 17:
		bitmap=(`cd $polybar_dir && ls *-Bitmap 2>/dev/null`)
                        ^-- SC2207 (warning): Prefer mapfile or read -a to split command output (or quote to avoid splitting).

For more information:
  https://www.shellcheck.net/wiki/SC2207 -- Prefer mapfile or read -a to spli...
