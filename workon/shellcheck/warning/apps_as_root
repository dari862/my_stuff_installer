
In /home/dari/Desktop/my_stuff/bin/rofi/apps_as_root line 8:
SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]:-$0}"; )" &> /dev/null && pwd 2> /dev/null; )";
^--------^ SC2034 (warning): SCRIPT_DIR appears unused. Verify use (or export if used externally).


In /home/dari/Desktop/my_stuff/bin/rofi/apps_as_root line 34:
    	$terminal)
        ^-------^ SC2254 (warning): Quote expansions in case patterns to match literally rather than as a glob.


In /home/dari/Desktop/my_stuff/bin/rofi/apps_as_root line 38:
    	$files)
        ^----^ SC2254 (warning): Quote expansions in case patterns to match literally rather than as a glob.


In /home/dari/Desktop/my_stuff/bin/rofi/apps_as_root line 42:
    	$editor)
        ^-----^ SC2254 (warning): Quote expansions in case patterns to match literally rather than as a glob.


In /home/dari/Desktop/my_stuff/bin/rofi/apps_as_root line 46:
    	$clifm)
        ^----^ SC2254 (warning): Quote expansions in case patterns to match literally rather than as a glob.


In /home/dari/Desktop/my_stuff/bin/rofi/apps_as_root line 50:
    	$vim)
        ^--^ SC2254 (warning): Quote expansions in case patterns to match literally rather than as a glob.


In /home/dari/Desktop/my_stuff/bin/rofi/apps_as_root line 57:
	Extra_Arg="$@"
                  ^--^ SC2124 (warning): Assigning an array to a string! Assign as array, or use * instead of @ to concatenate.


In /home/dari/Desktop/my_stuff/bin/rofi/apps_as_root line 64:
export SUDO_ASKPASS="$(rofi_askpass)"
       ^----------^ SC2155 (warning): Declare and assign separately to avoid masking return values.

For more information:
  https://www.shellcheck.net/wiki/SC2034 -- SCRIPT_DIR appears unused. Verify...
  https://www.shellcheck.net/wiki/SC2124 -- Assigning an array to a string! A...
  https://www.shellcheck.net/wiki/SC2155 -- Declare and assign separately to ...
